#To see how to connect your Docker Client to the Docker Engine
#running on this virtual machine, run: docker-machine env Char
eval $(docker-machine env Char)

# Now the ACTIVE column of Char has * instead of -
